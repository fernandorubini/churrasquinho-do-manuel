pluginManagement {
    repositories {
        google()  // Adiciona o repositório do Google
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositories {
        google()  // Adiciona o repositório do Google
        mavenCentral()
    }
}

include(":app")
